<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/logo_marrom_claro"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_marrom_claro"
        android:supportsRtl="true"
        android:theme="@style/Theme.Maitre"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".layout.AceiteLgpd"
            android:exported="false" />
        <activity
            android:name=".layout.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".layout.MainActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".layout.Home"
            android:exported="false" />
        <activity
            android:name=".layout.CriarConta"
            android:exported="false"></activity>
        <activity
            android:name=".layout.Login"
            android:exported="false"></activity>
    </application>
</manifest>